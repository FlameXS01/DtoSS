    Framework Principal: Next.js 15+ (con App Router)

    Lenguaje: TypeScript (en modo estricto)

    Estado del Servidor (Server State): TanStack Query

    Estado del Cliente (UI State): Zustand

    Estilos: Tailwind CSS

    Estructura de Carpetas: Feature-based (basada en funcionalidades)



src/
├── assets/
├── components/
│   ├── ui/               # Botones, inputs, etc.
│   └── layouts/          # Header, Footer, Sidebar (si los hay)
├── features/
│   ├── auth/             # Login, registro, logout
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── services/
│   │   ├── types/
│   │   └── index.ts
│   └── users/            # Gestión de usuarios
│       ├── components/    # UserList, UserCard, UserDetail, UserFilter
│       ├── hooks/         # useUsers, useUser, useFilterUsers
│       ├── services/      # usersApi.ts
│       ├── types/         # User, UserFilter, etc.
│       └── index.ts
├── hooks/                 # Hooks globales (ej: useAuth)
├── lib/                   # apiClient, queryClient
├── routes/                # AppRoutes, ProtectedRoute
├── stores/                # uiStore (tema, notificaciones), authStore (sesión)
├── types/                 # Tipos globales (si los hay)
├── utils/                 # formateadores, validadores
├── App.tsx
├── main.tsx
└── vite-env.d.ts